version: '3.8'

services:
  permission-protector:
    build:
      context: .
      dockerfile: Dockerfile.windows
    ports:
      - "3000:3000"
      - "8080:8080"
    volumes:
      - type: bind
        source: C:\
        target: C:\host
        read_only: true
    environment:
      - DATABASE_URL=sqlite:///app/data/permission-protector.db
    restart: unless-stopped

  # Alternative Linux-based service for cross-platform
  analyzer-linux:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
      - "8081:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///app/data/permission-protector.db
    restart: unless-stopped
    profiles: ["linux"]
